[package]
name = "topology"
version = "0.1.0"
description = "Resolves the device topology of a system that already exists"
edition.workspace = true
rust-version.workspace = true

[dependencies]
disks = { path = "../disks" }
superblock = { path = "../superblock" }
types = { path = "../types" }
log.workspace = true
thiserror.workspace = true

[dev-dependencies]
test-log.workspace = true
